<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <style>
        .my-form {
            position: fixed;
            bottom: 24px;
            right: 80px;
            left: 80px;
            width: calc(100% - 160px);
        }

        .date {
            font-size: 10px;
            position: absolute;
            right: 4px;
            bottom: 4px;
            color: rgba(0, 0, 0, 0.5)
        }
    </style>
</head>

<body>
    <nav class="navbar bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Chat Messanger</a>
        </div>
    </nav>

    {% for message in messages %}
    <div class="d-flex {% if name == message.user %} justify-content-start {% else %} justify-content-end {% endif %} mt-5">
        <div class="card">
            <div class="card-body">
                <b>{{message.user}}:</b> {{message.content}}
                <span class="date">{{message.created_at.strftime('%d.%m.%Y %H:%M')}}</span>
            </div>
        </div>
    </div>
    {% endfor %}

    <form class="input-group mb-3 my-form" action="" method="POST">
        <input type="text" name="content" class="form-control" placeholder="Bitte gebe deine Nachricht an">
        <button class="btn btn-outline-secondary" type="submit">Senden</button>
    </form>

</body>

</html>